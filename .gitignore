COPILOT.md
CLAUDE.md
.vscode
.claude
target/
.github/copilot-instructions.md
dist/
node_modules
*.png
test-results
bench/native/build/
bench/graphs/.venv/
__pycache__/
results/
.terraform
*.tfstate
*.tfstate.*
*.tfvars
crash.log
# Local AWS creds for infra/cloud_bench.sh — never commit. .env.example is the
# tracked template (negated so it stays in the repo).
.env
!.env.example
# Transient state for the decoupled cloud_provision/cloud_collect flow
.cloud_run.env
